# Copy to application-local.properties for custom local overrides (gitignored).
# Or use the committed dev profile (recommended):
#
#   mvn spring-boot:run -Dspring-boot.run.profiles=dev
#
# spring.profiles.active=dev
