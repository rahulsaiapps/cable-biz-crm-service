# ========================================================================
# Cable Pulse - Core Spring Boot Backend Production Properties
# ========================================================================
spring.application.name=cable-pulse

# --- HIKARI CONNECTION POOL SETTINGS (SUPABASE POSTGRESQL) ---
spring.datasource.url=jdbc:postgresql://db.pbuchneyuekfvweehdpg.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=IZSFOhzr@.a123
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / HIBERNATE PERFORMANCE OPTIMIZATIONS ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# --- SECURITY BOUNDARY CONFIGURATION ---
cablepulse.firebase.config-path=classpath:firebase-service-account.json
